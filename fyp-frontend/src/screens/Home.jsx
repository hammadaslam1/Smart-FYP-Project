import { Card, Typography } from "@mui/material";
const Home = () => {
    return (
        <Card>
            <Typography variant="h2">Ahsan</Typography>
        </Card>
    );
};

export default Home;
